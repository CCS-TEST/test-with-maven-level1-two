package ccs.test.mvn.level1.two;

/**
 * An Armstrong number is a number such that the sum
 * of its digits raised to the third power is equal to the number
 * itself.  For example, 371 is an Armstrong number, since
 * 3**3 + 7**3 + 1**3 = 371.
 *
 *
 *
 * Find out how to do power (exponent) from: https://docs.oracle.com/javase/8/docs/api/java/lang/Math.html
 *
 */
public class ArmstrongNumber {

    /**
     * This method must throw an exception is the integer passed is not a 3 digit number.
     *
     * ====================================================
     * We are only focussing on 3 digit armstrong number
     * ====================================================
     *
     * @param input the input
     * @return true if it is armstrong number, false otherwise.
     * @throws BadArgumentException
     */
    public static Boolean is3DigitArmstrongNumber(int input) throws BadArgumentException {
        return null;
    }
}
