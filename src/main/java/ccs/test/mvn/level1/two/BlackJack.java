package ccs.test.mvn.level1.two;

/**
 * Given 2 int values, return whichever value is nearest to 21 without going over.
 * Return 0 if they both go over.
 */
public class BlackJack {
    /**
     * Given 2 int values, return whichever value is nearest to 21 without going over.
     * Return 0 if they both go over.
     *
     * @param a the first number
     * @param b the second number
     * @return
     * @throws BadArgumentException  if any one of them is negative
     */
    public static  int blackjack(int a, int b) throws BadArgumentException {
        return -1;
    }

}
